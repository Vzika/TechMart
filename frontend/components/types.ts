export interface Product {
  id: number;
  name: string;
  price: number;
  description: string;
  // Add other product fields as needed
}

export interface User {
  email: string;
  id: number;
  username: string;
}
